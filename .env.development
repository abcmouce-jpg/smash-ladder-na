# Local development defaults — works out of the box with `docker compose up -d`.
# Copy to .env or just run `npm run dev` (Next.js loads .env.development automatically).
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/smashladder"
DIRECT_URL="postgresql://postgres:postgres@localhost:5432/smashladder"
DATABASE_URL_POOLED="postgresql://postgres:postgres@localhost:5432/smashladder"
AUTH_SECRET="dev-secret-do-not-use-in-production"

# Opt-in local-dev showcase: set to 1 to treat seeded users' Twitch channels as
# live so the home page carousel, the Live page's pinned player, and the profile
# embeds can be demoed without real channels. Off by default so local dev matches
# production — only channels that are actually live are shown. Ignored in
# production either way (see MOCK_LIVE_TWITCH handling in lib/twitch-helix.ts).
MOCK_LIVE_TWITCH=0

# Local-dev web push keys — generate fresh ones for prod with:
#   npx web-push generate-vapid-keys --json
NEXT_PUBLIC_VAPID_PUBLIC_KEY=""
VAPID_PRIVATE_KEY=""
VAPID_SUBJECT="mailto:admin@smash-ladder-na.local"
